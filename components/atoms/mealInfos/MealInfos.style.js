import { StyleSheet } from 'react-native';

const S = StyleSheet.create({
  details: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    padding: 8,
  },
  detailEl: {
    marginHorizontal: 4,
    fontSize: 12,
  },
});

export { S };
